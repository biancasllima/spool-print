#!/bin/bash

# This bash script will be used to test commands and bash utilities
# Do such thing to maintain the projects behavior

echo $INIT

# salvar da mesma forma que os demais arquivos os logs
# preservar a experiência de /bin/bash: ao utilizar os comandos e CTRL-D = (EOF) exits the shell
